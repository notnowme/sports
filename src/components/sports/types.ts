export interface MatchData {
    ''? : string
    matchDate: string
    matchTime: string
    matchPlace: string
    matchTeams: string[]
    matchResult?: string[]
}

export interface GoodsData {
    team: string
    goodsPrice: string
    goodsName: string
    goodsImg: string
}